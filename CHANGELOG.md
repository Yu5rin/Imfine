<!--
  このファイルの形式について:
  - 各バージョンの見出しは「## v1.12.0」のように、リリースタグ名と完全一致させること。
  - 新しいバージョンを上に追記していく（降順）。
  - リリース時、GitHub Actions ワークフローが「## v<バージョン>」の次の行から
    次の「## 」見出しの直前までを自動で抜き出し、リリースノートとして使用する。
    見出しが一致しないとワークフローがビルド前にエラーで停止する。
-->

## v1.12.0

見た目と動作の総点検を行い、見つかった問題をまとめて修正しました。

### 重要な修正

**自動アップデート後に ON が解除されてしまう問題を修正**

ON にしたまま自動アップデートが実行されると、更新後の再起動で OFF に戻ってしまい、モニターがスリープしたり在席状態が保てなくなることがありました。更新前の状態を記録し、再起動後に自動で復元するようにしました。

**停止時刻の変更が保存されない問題を修正**

停止時刻の欄に `7` のように 1 桁で入力すると、表示は `07` に整形されるものの設定が保存されず、次回起動時に元の値に戻ってしまっていました（v1.9.2 から発生していた不具合です）。範囲外の値を入力した場合（`99` → `23`）も同様でした。

**設定が消えてしまう問題を修正**

停止時刻の欄が空欄など不正な状態でテーマを切り替えると、他の設定（停止時刻・トレイ格納・自動 ON・ウィンドウ位置）がすべて初期値に戻ってしまう問題を修正しました。また、設定ファイルの書き込み方法を改善し、書き込み中に電源が落ちても設定が壊れないようにしました。

### 新機能

- **起動時にタスクトレイへ格納**するオプションを追加しました。Windows 起動時の自動起動と組み合わせると、ログオン後にウィンドウが出てこなくなります
- **自動アップデートの ON / OFF** を設定画面から切り替えられるようになりました
- タスクトレイの右クリックメニューに**「更新を確認」**を追加しました
- 更新のダウンロード中・適用中が画面とトースト通知で分かるようになりました
- 停止時刻に不正な値を入力すると入力欄が色付きになり、原因が分かるようになりました

### 改善

- ウィンドウの × ボタンでタスクトレイに格納されるようになりました（終了はトレイメニューから行えます）
- 動作状態の文字が薄くて読みにくかったのを改善しました
- 文字サイズを整理し、小さすぎて読みにくい箇所をなくしました
- 高解像度ディスプレイ（150%・200% 表示）でトグルスイッチだけ小さいままだったのを修正しました
- ダークテーマでチェックボックスの周囲に白い枠が出ていたのを修正しました
- 設定ファイルが壊れている場合でもエラー画面を出さず、初期値で起動するようにしました
- マルチモニター環境でウィンドウ位置が復元されるようになりました

### セキュリティ

- 更新の取得先を GitHub に限定しました。設定ファイルを書き換えても、別のサーバーから更新を取得させることはできません
- README に、SHA256 検証が「通信中の破損検知」であって「改ざん検知」ではないこと、および「アクティブ状態を維持」の実際の動作（F15 キーとマウス 1px 移動の送信）を明記しました

## v1.11.1

### 変更

**更新確認を起動のたびに毎回行うように**

これまでは起動時に最大1日1回までの間引きをしていましたが、起動するたびに毎回確認するように変更しました。

**一時ファイルが残る不具合を修正**

自動アップデート処理で、まれな失敗経路（新しい exe への差し替え失敗、再起動プロセスの起動失敗）が発生した場合に、ダウンロード済みの一時ファイルが削除されずに残ることがありました。いずれの場合も確実に削除するよう修正しました。通常の更新が成功するケースには影響ありません。

## v1.11.0

### 追加機能

**完全自動アップデート**

起動時に新しいバージョンがないか自動で確認します。新しいバージョンが見つかった場合、確認ダイアログなしで以下を自動的に行います。

1. 新しい exe をダウンロード
2. SHA256 でファイルの整合性を検証
3. 検証に成功した場合のみ、実行中の exe を新しいものに置き換え
4. アプリを自動的に再起動

検証に失敗した場合（通信エラー・改ざん・破損など）は何も行わず、現在のバージョンのまま動作を継続します。

### その他

- リポジトリを Public に公開し、MIT ライセンスを適用しました
- 依存ライブラリのライセンス表記（THIRD-PARTY-NOTICES.md）を追加しました

## v1.10.1

### 追加機能

**タイトルバーの色がテーマに連動するように**

Windows 11 の DWM API を使い、ウィンドウのタイトルバーをアプリのライト/ダークテーマに合わせて自動で色付けするようにしました。ダークテーマ選択時はシステムメニューなどの見た目もダークに統一されます。

- 対応環境: Windows 11 22H2（ビルド 22621）以降
- それ未満の環境（Windows 10 や Windows 11 初期版）では、従来通り OS 標準のタイトルバーで動作します（エラーは出ません）
