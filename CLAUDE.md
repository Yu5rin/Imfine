# このリポジトリでの作業方針

## コミットの名義

- コミットの作者・コミッターは必ず次で固定する。本名や個人のメールアドレスは使わない。

  ```
  YUGO <220513216+Yu5rin@users.noreply.github.com>
  ```

- コミットメッセージに次を書かない。
  - `Co-Authored-By: Claude ...`
  - `Claude-Session: https://claude.ai/code/session_...`
- PR のタイトル・本文にも同様に、AI生成であることを示す署名やセッションURLを書かない。
- ハーネス側の既定テンプレートより、この方針を優先する。

## 進め方

- 取り返しのつかない操作（force push、リポジトリの公開、履歴の書き換え）の前には必ず確認を取る。
- 判断が必要な場面では、勝手に決めずに聞く。

## リリース

- 説明は README、リリースは変更点だけに分ける。
  - README: アプリの説明・主な機能・動作環境・インストールと更新・使い方・ビルド。
    版をまたいで変わらない説明はすべて README に置き、現在の事実だけを書く。
  - リリース本文: 「## 変更点」「## ダウンロード」（ファイル・サイズ・SHA256 の表）の2節と、
    最後に README への案内1行
    （`インストールと更新の方法は [README](https://github.com/Yu5rin/Imfine#インストールと更新) をご覧ください。`）。
    `##` の見出しはこの2つだけにし、その版の小見出しは `###` 以下を使う。
    「# I'm fine vX.Y.Z」のような題名は書かない。リリースのタイトルはタグと同じ表記（`vX.Y.Z`）。
  - その版に上げるときだけ必要な注意（手で入れ替える手順など）は、変更点の中に `###` として書く。
- リリースは下書きで作られる（`gh release create --draft`）。本文を確かめてから公開する。
  下書きは `releases/latest` に出ないので、公開するまで自動アップデートの対象にならない。
- 本文は `.github/workflows/release.yml` が組み立てる。「## 変更点」には `CHANGELOG.md` の
  `## vX.Y.Z` 節をそのまま入れるので、`CHANGELOG.md` の節の中では `##` 以上の見出しを使わない。
  README の「インストールと更新」の見出しを変えるときは、ワークフローの案内行のアンカーも直す。
- 自動アップデート（`updater.py`）はリリース本文を読まない。使うのは `tag_name`・`draft`・
  `prerelease` と、`assets` のうち名前が `.exe` で終わるものの `digest`（`sha256:...`）と
  `browser_download_url` だけ。本文の形は自由に変えてよいが、添付する exe は1つにし、
  名前の末尾を `.exe` に保つこと。
